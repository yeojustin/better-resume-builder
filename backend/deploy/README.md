# Backend deploy artifacts

Dockerfile, Compose files, and Kubernetes examples for this service live in this folder.

**Step-by-step production deployment** (environment variables, Docker, Kubernetes outline, frontend build): see **`DEPLOY.txt`** at the **repository root**.

For Compose-only variable substitution on the host, you may use `env.example` in this directory (do not commit secrets).
