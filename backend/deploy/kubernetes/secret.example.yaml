# Template only — create real secrets via `kubectl create secret` or your cloud provider.
# Do not commit files containing real keys.
apiVersion: v1
kind: Secret
metadata:
  name: brb-gemini
type: Opaque
stringData:
  GEMINI_API_KEY: "replace-me"
